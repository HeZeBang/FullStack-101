---
title: "Quick Start Guide"
author: "Bob Johnson"
date: "2025-06-27"
tags: ["quickstart", "tutorial", "beginner"]
description: "Get up and running with MDX in minutes"
---

export const timeToComplete = "5 minutes";
export const difficulty = "Beginner";

# Quick Start Guide

Get started with MDX in just {timeToComplete}!

Difficulty Level: **{difficulty}**

## Step 1: Installation

```bash
npm install @mdx-js/react @mdx-js/mdx
```

## Step 2: Basic Setup

Create your first MDX file and start writing content that combines the best of Markdown and React.

{/* Quick Example Section */}

---
section: "first-example"
type: "tutorial"
---

export const exampleTitle = "Your First MDX Component";

# {exampleTitle}

Here's a simple example to get you started:

```mdx
# Hello MDX!

This is **bold** text and this is *italic* text.

export const greeting = "Hello World!";

## Dynamic Content

{greeting}
```

{/* Configuration Section */}

---
section: "configuration"
complexity: "simple"
---

export const configSteps = ["Install dependencies", "Configure bundler", "Create MDX files", "Start building"];

# Configuration

Follow these steps:

{configSteps.map((step, index) => `${index + 1}. ${step}`).join('\n')}

## Next Steps

Once you have the basics working, you can explore more advanced features like:

- Custom components
- Plugins and extensions  
- Advanced styling
- Performance optimization
